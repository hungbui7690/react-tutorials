/*
  useRef: Initial Setup
  > Characteristics:  
    - preserves value
    - DOES NOT trigger re-render
    - target DOM nodes/elements

*/

import React, { useEffect, useRef } from 'react'

const UseRefBasics = () => {
  return <h2>useRef</h2>
}

export default UseRefBasics
